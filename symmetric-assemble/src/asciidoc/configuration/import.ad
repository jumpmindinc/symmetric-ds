
=== Import

SymmetricDS configuration can be imported via the Import button in the web console if it is in CSV or SQL format.

WARNING: This will wipe out all existing configuration settings and replace with the newly imported ones.

If the configuration is in SQL format, another option is to run the SQL script via database specific tools.